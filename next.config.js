/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // IMPORTANT: Do NOT set `output: 'export'` on Vercel for App Router pages.
  // No basePath or weird rewrites for now.
};

module.exports = nextConfig;
